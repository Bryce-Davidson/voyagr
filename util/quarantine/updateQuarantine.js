const keysContainString = require('../local-functions/instanceValidation').keysContainString

module.exports = async function(update) {
    // take in update
    // look for immutable paths
        // if found immutable
}